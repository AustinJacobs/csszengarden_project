{
  "name": "csszengarden_projet",
  "version": "1.0.0",
  "description": "This is the project directory for the main css zen garden project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "rimraf build/*",
    "start": "parcel src/index.html --dist-dir build",
    "prebuild": "rimraf dist/*",
    "build": "parcel build src/index.html --public-url ./",
    "watch": "parcel watch src/index.html -dist-dir build/"
  },
  "author": "Austin Jacobs, Aarom Cram, Emmanuel Gyasi-Ampomah",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0-beta.2",
    "parcel": "*",
    "parcel-bundler": "^1.12.5",
    "rimraf": "^3.0.2"
  }
}
